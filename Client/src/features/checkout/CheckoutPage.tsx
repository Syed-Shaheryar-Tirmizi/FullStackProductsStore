import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <Typography variant="h2">
            Checkout page (visible to only logged in users)
        </Typography>
    )
}